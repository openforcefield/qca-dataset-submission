name: qcarchive-worker-openff-ani
channel_priority: flexible
channels:
  - conda-forge
  - pytorch-nightly
  - defaults
dependencies:
  - python =3.7
  - qcfractal
  - qcengine =0.16.0

  # ML calculations
  - pytorch =1.6.0

  # procedures
  - geometric

  # compute backends
  - dask-jobqueue

  - pip:
    - torchani =2.2
